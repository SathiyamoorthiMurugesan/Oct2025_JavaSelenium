package testPractice_Java;

//Only public and default (implicit) is allowed inside public interface

public interface Test_A2_CreatingInterfaces {
	
	interface A1 {
		
	}
	
}

//we can create a new/diff interfacename using a default(implicit) 
//but public interface must be defined in its own file 

interface Test_A2_CreatingInterfaces_New{
	
}